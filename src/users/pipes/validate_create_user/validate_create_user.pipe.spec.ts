import { ValidateCreateUserPipe } from './validate_create_user.pipe';

describe('ValidateCreateUserPipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUserPipe()).toBeDefined();
  });
});
